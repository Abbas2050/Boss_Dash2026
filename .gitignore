# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
.env
.env.*
!.env.example

# Secrets / keys
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*credentials*.json
*secret*.json
*service-account*.json
skylinkscapital-12ac4c6a138f.json
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local runtime SQLite DB (must stay untracked to avoid deployment lock failures)
crm_mt5.db

# Local wallet token cache
storage/bitpace_token.json

# Vite dependency pre-bundle cache — regenerated on every dev run.
# Was tracked, which produced large meaningless diffs on every restart.
.vite-cache/

# Runtime state written by the server (reconcile cursors, report state,
# generated report images). Machine-local and environment-specific; every
# reader recreates it when absent, so it must not be version controlled.
storage/

# Brainstorming visual companion artifacts
.superpowers/
