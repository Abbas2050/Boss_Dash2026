<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <!-- PassThrough: keep Express error bodies (JSON) instead of IIS HTML pages -->
    <httpErrors existingResponse="PassThrough" />

    <!-- URL Rewrite: send every request that is NOT a physical file to Express.
         Plesk already configures iisnode — do NOT add a <handlers> section here.
         This rule ensures /assets/index.js, /skylinks.ico, etc. reach Express
         which then serves them from dist/ via express.static.                  -->
    <rewrite>
      <rules>
        <rule name="Route all non-file requests to Express" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
          </conditions>
          <action type="Rewrite" url="server.js" />
        </rule>
      </rules>
    </rewrite>

    <!-- Block direct browser access to sensitive server-side directories -->
    <security>
      <requestFiltering>
        <hiddenSegments>
          <add segment="node_modules" />
          <add segment=".git" />
          <add segment="src" />
          <add segment="auth" />
          <add segment="docusign" />
          <add segment="scripts" />
        </hiddenSegments>
      </requestFiltering>
    </security>
  </system.webServer>
</configuration>
